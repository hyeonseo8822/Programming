# Programming 소스코드
## 02_counter
vanilla HTML, javaScript

## React 시작
```shell
npm create vite@latest . 
```

### CounterApp
 - `useState(초기값)`
 - 'onClick = {() => set함수(바꿀값)}`
 - 'onClick = {() => set함수((이전state) => 이전state + 1)}`
 - 'onClick = {함수이름}`
